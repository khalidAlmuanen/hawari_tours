# 🚀 Production Deployment Commands
# أوامر نشر البيئة الإنتاجية

## مشكلة: الموقع يعمل على localhost لكن لا يعمل في الإنتاج
# الحل: تحديث قاعدة بيانات الإنتاج بنفس التغييرات

## الخطوات المطلوبة:

### 1. تحديث متغيرات البيئة في الإنتاج
تأكد من أن DATABASE_URL صحيح في الإنتاج

### 2. تطبيق المخطط الجديد على الإنتاج
```bash
# في بيئة الإنتاج
npx prisma migrate deploy
npx prisma generate
```

### 3. تحميل البيانات في الإنتاج
```bash
# تحميل البيانات الأساسية
node seed-simple.js

# إضافة البيانات الإضافية
node add-socotra-data.js
```

### 4. إعادة تشغيل التطبيق
```bash
# إعادة تشغيل الخادم
npm run build
npm start
```

## ملاحظات هامة:
- تأكد من أن متغيرات البيئة في الإنتاج صحيحة
- قم بعمل نسخة احتياطية من قاعدة البيانات قبل التغييرات
- تحقق من الاتصال بقاعدة البيانات الإنتاجية
